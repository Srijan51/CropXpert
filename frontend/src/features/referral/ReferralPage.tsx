/**
 * Referral page — escalation to extension officers and labs.
 */

import React from "react";

const ReferralPage: React.FC = () => {
  return (
    <div>
      <h2 className="text-2xl font-bold text-gray-800">Referral to Extension Officer</h2>
      {/* TODO: Implement referral form and status tracking */}
    </div>
  );
};

export default ReferralPage;
